[db]
host = "custom_db_host"
user = "custom_db_user"
password = "custom_db_password"

[chrome]
# optional, default to true
# headless = true
# optional, default to 5
concurrent_tabs = 4
# optional, default to a random dir in /tmp
data_dir = "/path/to/data"
